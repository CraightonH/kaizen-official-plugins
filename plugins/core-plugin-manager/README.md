# core-plugin-manager

Exposes a runtime plugin-manager API for load/unload/reload.

## Permissions

trusted

## Configuration

See plugin `config.schema` in `index.ts` for the authoritative configuration shape.

## Capabilities

See plugin `capabilities` in `index.ts`.
