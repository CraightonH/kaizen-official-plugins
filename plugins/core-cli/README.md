# core-cli

Registers configured CLIs as kaizen tools. Each CLI becomes a subprocess-backed tool.

## Permissions

scoped (exec)

## Configuration

See plugin `config.schema` in `index.ts` for the authoritative configuration shape.

## Capabilities

See plugin `capabilities` in `index.ts`.
