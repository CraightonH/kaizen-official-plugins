# core-executor-openai

OpenAI LLM executor backed by `@ai-sdk/openai`.

## Permissions

scoped (net: api.openai.com:443, env: OPENAI_API_KEY)

## Configuration

See plugin `config.schema` in `index.ts` for the authoritative configuration shape.

## Capabilities

See plugin `capabilities` in `index.ts`.
