# core-executor-anthropic

Anthropic LLM executor backed by `@ai-sdk/anthropic`.

## Permissions

scoped (net: api.anthropic.com:443, env: ANTHROPIC_API_KEY)

## Configuration

See plugin `config.schema` in `index.ts` for the authoritative configuration shape.

## Capabilities

See plugin `capabilities` in `index.ts`.
