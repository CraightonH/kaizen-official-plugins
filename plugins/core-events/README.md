# core-events

Defines the canonical event vocabulary (`session:*`, `tool:*`) for kaizen sessions.

## Permissions

trusted

## Configuration

See plugin `config.schema` in `index.ts` for the authoritative configuration shape.

## Capabilities

See plugin `capabilities` in `index.ts`.
