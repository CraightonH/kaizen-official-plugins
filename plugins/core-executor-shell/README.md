# core-executor-shell

Shell-pipe executor — pipes prompt over stdin, returns stdout.

## Permissions

scoped (exec)

## Configuration

See plugin `config.schema` in `index.ts` for the authoritative configuration shape.

## Capabilities

See plugin `capabilities` in `index.ts`.
