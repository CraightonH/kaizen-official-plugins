# timestamps

Prefixes user and agent messages with ISO-8601 timestamps.

## Permissions

scoped (events)

## Configuration

See plugin `config.schema` in `index.ts` for the authoritative configuration shape.

## Capabilities

See plugin `capabilities` in `index.ts`.
