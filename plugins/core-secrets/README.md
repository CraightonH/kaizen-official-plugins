# core-secrets

Pluggable secrets backend with OS keychain detection + file fallback.

## Permissions

trusted

## Configuration

See plugin `config.schema` in `index.ts` for the authoritative configuration shape.

## Capabilities

See plugin `capabilities` in `index.ts`.
