## Completed
- session target: llm-driver
- session target: llm-events
- session target: llm-tui
- session target: llm-system-prompt
- session target: llm-status-items
- session target: openai-llm
- session target: llm-tools-registry
- session target: llm-tools-registry (ToolSource open-shape refactor — bumped to 0.3.0; llm-codemode bumped to 0.2.0)
- session target: llm-codemode (bumped to 0.3.0; dropped dead `maxBlocksPerResponse` and `public.d.ts`; narrowed `services.consumes`; extracted shared `kaizen-tree.ts` helper to lock host/worker grouping shape)
- session target: llm-session-manager (bumped to 0.1.1; canonical tsconfig; fingerprint from package.json; async EventHandler signatures; local HarnessIdentity since kaizen pkg no longer exports it; canonical ToolCall shape in tests; root smoke test)
- session target: llm-local-tools (bumped to 0.2.0; setup→stop() canonical lifecycle [was returning {teardown}, never invoked — actual bug]; wired ripgrep shell-out path that README claimed but never ran; bash/grep type fixes; canonical tsconfig; README/CLAUDE.md corrected — edit schema, web_fetch section, 7→8 tool count; contract-change doc filed)
- session target: llm-tavily-search (no version bump; lifecycle moved to canonical stop(); ToolSchema/ToolExecutionContext imported from llm-tools-registry/public — narrowest stable contract owner; listener leak fix on abort; canonical tsconfig; README Services/Config/Permissions/Errors sections added)
- session target: llm-mcp-bridge (bumped to 0.1.2; canonical tsconfig; VERSION from package.json; dropped vestigial `register:` from internal RegistryLike — lifecycle/service only used registerWith; README documents LLM-callable peer tools and per-tool source.kind provenance; ToolSource conformance verified — proxied tools as `{kind:"mcp", server}`, control tools as `{kind:"local"}`)
- session target: llm-skills (bumped to 0.1.2; canonical tsconfig; added missing llm-events + llm-system-prompt workspace deps; module-scope let + idempotent stop() lifecycle; dropped dead try/catch around useService and dead register() fallback; CLAUDE.md fix — public.d.ts owns its types, not a re-export)
- session target: llm-memory (bumped to 0.1.2; canonical tsconfig; narrowed services.consumes to ["llm-events:vocabulary"] only per AGENTS.md — optional services via useService; added idempotent stop(); typed turn:end payload; tools wrap store.put returning structured `{ok:false, error}` instead of throwing; contract-change doc filed)
- session target: llm-agents (bumped to 0.2.1; canonical tsconfig; missing workspace deps declared; narrowed services.consumes to llm-events:vocabulary only — optional services via useService with harness:error degradation; module-scope handles + idempotent stop(); ToolSchema imported from llm-tools-registry/public; bumpSection wired into pre-discovery registry; ctx.emit captured at setup() — dropped (ctx as any) hacks)
- session target: llm-slash-commands (bumped to 0.2.1; canonical tsconfig; idempotent stop() on completion-source unregister; typed payload narrowing on input:submit; README/CLAUDE.md corrected built-ins — /help, /exit, /history only; soft-backdoor of llm-session-manager registering /clear with source:"builtin" flagged for future registry hardening)
- session target: llm-native-dispatch (bumped to 0.3.1; canonical tsconfig; added llm-events workspace dep; strategy.ts narrowed `as any` error handling to instanceof Error typeguard; CLAUDE.md install-dir uses <version> placeholder; tsconfig no longer excludes driver-integration.test.ts after llm-driver follow-up)
- session target: llm-hooks-shell (bumped to 0.1.1; canonical tsconfig; added llm-events workspace dep; CANCEL_TOOL/CODEMODE_CANCEL_SENTINEL imported from llm-events owner instead of redefined locally — removes drift risk; security posture re-validated)
- follow-up: llm-driver (bumped to 0.2.1; declared workspace deps llm-events + llm-session-manager that were imported but missing — unblocked native-dispatch's driver-integration tsc; CLAUDE.md install-dir bumped; refactor-opportunities doc filed for the agent-persona override bug and the ecosystem-wide services.consumes accuracy tension)
