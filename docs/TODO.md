1. Add tool call approval gate plugin. Subscribe to tool:before (or similar) event and intercept the request. Present a prompt to the user if they approve of the call - Yes/No option navigated with up/down arrow, enter to submit. Place the UI above the inputbox. Show the tool request in human readable format, ie. not the raw json.
2. Have TUI detect markdown in the LLM's response and default to markdown rendering. Then implement a keyboard shortcut to copy the raw text from the latest message, and a keyboard shortcut to copy the rendered markdown from the latest message, if possible.
3. What does it look like to build plugin services as OpenAPI compliant API surfaces? It would be cool to have sufficient documentation that a plugin consumer could easily read a given plugin's API. Such docs should live within plugin scope. It would be interesting to investigate an aggregate OpenAPI doc at a harness level to see the API surface of all declared plugins, but this would be exploratory and I'm not even sure where that would live.
4. REFACTOR: AGENTS.md describes the service architecture I've always envisioned, now encoded into coherent instructions. Evaluate all openai-compatible harness' plugins to ensure they align with this architecture. At a high level this means that 1. Any plugin providing a service that is consumed by another should not own the service definition, and 2. Any plugin providing a service should provide the service in a way that assumes the implementation could be replaced by a new plugin. 